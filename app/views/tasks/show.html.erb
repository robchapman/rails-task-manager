<h1><%= @task[:title] %></h1>
<p><%= @task[:details] %></p>
<br>
<p><%= link_to("Edit", edit_task_path(@task)) %></p>
<%= link_to 'Delete',
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<br>
<p><%= link_to("Back to List", tasks_path) %></p>
